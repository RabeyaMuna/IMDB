<%= form_with(model: [@post, @post.post_ratings.build]) do |f| %>
   <%= f.number_field :rating, require: true, size: 5, min: 0, max: 5, placeholder: 'Rate the Movie 1 to 5'%>
   <%= f.submit '*', class: 'btn btn-primary' %>
<%end%>
